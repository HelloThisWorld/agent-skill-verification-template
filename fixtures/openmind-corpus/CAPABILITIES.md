# Capability registry

Queries are routed to exactly one of these capabilities. The router may never
select a capability outside this set.

- glossary: resolve a term or acronym to its verbatim, source-cited definition.
- structure: answer code structure questions — callers, dependencies, modules, flow.
- search: find code by concept or identifier across the corpus.
