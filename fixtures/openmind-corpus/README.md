# LedgerLab

LedgerLab is a tiny double-entry bookkeeping service used as a verification
corpus. Invoices settle via straight-through processing (STP) before postings
are written to the ledger.

Run it with `python app/main.py`.
